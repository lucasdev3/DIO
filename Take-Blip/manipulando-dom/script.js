const frase = 'lifes not a problem to be soved.';
const n = 14;

if (n>=1 && n<=34) {

  const fraseParcial = texto.substr(0, n)
  print(fraseParcial.toUpperCase());
  console.log(fraseParcial.toUpperCase());

}