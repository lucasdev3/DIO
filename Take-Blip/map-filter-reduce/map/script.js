// usando map

const array = [1, 2, 3, 4, 5];

const newArray = array.map((item) => item * 2); // retorna um novo array


// usando foreach

const array2 = [1, 2, 3, 4, 5];

const newArray2 = array2.forEach((item) => item * 2); // retorno indefinido

console.log(newArray);
console.log(newArray2);

